# 看影音更新弹窗文案

## 当前版本

- 应用版本：2.1.145
- Windows EXE 安装器版本：2.1.145 测试版
- 本轮交付：Windows 测试版 EXE；Android 测试版 APK/AAB
- Android 应用版本：2.1.145
- Android versionCode：20145
- 日期：2026-08-07

## 弹窗标题

看影音 2.1.145 测试版

## Windows 弹窗正文

- Android TV 海报墙改为更紧凑的四列友好布局，可以同时浏览更多作品。
- 子页面最左侧继续按左键可进入侧边导航栏，闭环焦点组不再困住遥控器焦点。
- TV 返回键可退出输入状态、弹窗和子页面；播放时先收起控制层，再次返回才离开播放页。
- `.kyyconfig` 和 `.kyymeta` 兼容不提供完整文件名的电视文件管理器。
- 手机扫码配置页面可以选择并上传 `.kyyconfig` 与 `.kyymeta`，电视端确认后导入并显示明确结果。
- Android TV 导入 `.kyymeta` 和 `.kyyconfig` 时使用可遥控的系统文件选择器，文件会先流式复制到应用缓存，读取后自动清理。
- 网盘目录页顶部的“选择当前目录”和“确定”现在有清晰焦点，选中当前目录后可按右键确认。
- 新增密码加密的 `.kyyconfig` 导入导出，可在设备间迁移 TMDB Key、个人网盘来源和凭据。
- 支持 OpenList、夸克、百度和迅雷网盘来源；配置按来源 ID 合并，未包含的现有来源会保留。
- 空 TMDB Key 不会清除当前设备的 Key；任何写入失败都会恢复导入前的 TMDB、来源和凭据。
- 没有 TMDB Key、断网或请求失败时，本地扫描、媒体库浏览和播放仍可继续。
- Windows 安装向导始终提供目录选择页，用户选择的目录就是程序实际安装目录。
- 配置迁移不会修改或删除视频、字幕、索引、缓存和播放历史，也不会重命名文件或改变播放路径。

## Android 弹窗正文

### 弹窗标题

看影音 Android 2.1.145 测试版

- Android TV 海报墙缩小卡片和间距，电影、动漫、电视剧与本地媒体库保持一致。
- 子页面最左侧可进入侧边导航栏，返回键可退出输入框、弹窗和底部面板。
- 播放器首次返回收起控制层，再次返回离开播放页，不再先切换电视屏幕模式。
- 导入配置和刮削资料兼容没有完整显示名的电视文件管理器。
- Android TV 设置操作现在显示高对比焦点边框、浅色背景、勾选图标和确认提示。
- 手机扫码配置必须让电视与手机连接同一局域网，不需要部署服务器；电视端会显示连接、确认、写入和成功或失败状态。
- 手机可配置 OpenList、夸克、百度和迅雷网盘来源及个人凭据，新增非 OpenList 来源后仍需在电视选择媒体目录。
- 新增密码加密的 `.kyyconfig` 导入导出，配置按来源 ID 合并，写入失败会回滚。
- 导入和扫码配置不会修改或删除视频、字幕、索引、缓存和播放历史。
- Windows 和普通 Android 保留原有文本输入与焦点行为。
- Android 新增电影、动漫和电视剧分类入口，可直接浏览本地与个人网盘资源。
- 剧集支持逐视频匹配 TMDB 季度和集数，选集显示“当前剧名 S01E01 TMDB 集名”。
- TMDB 集名只用于补充单集名称，不会覆盖用户设置的剧名，也不会修改本地文件、网盘路径、字幕关联或播放入口。
- 多季度、多版本和不同目录的作品会按统一规则归并，并保留对应季度海报、集数和播放资源。
- TMDB 请求使用共享缓存并合并重复请求；没有 TMDB Key、断网或请求失败时，扫描、浏览和播放仍可使用。
- Android 继续支持字幕、音轨、后台播放、画中画、MediaCodec 硬件解码和 Anime4K。
- 本次更新不会修改或删除本地及网盘原始视频、字幕或其他文件，也不会改变远程路径和播放 ID。
- Android TV 测试包使用 `tvTest` flavor；构建与包验证通过，海信实机验收未完成。

## 按钮文字

知道了

## 维护要求

发布新版本时，本文件中的应用版本、EXE 安装器版本和弹窗正文必须与以下文件保持一致：

- `pubspec.yaml`
- `lib/core/app_version.dart`
- `lib/utils/version_history.dart`
- `RELEASE_NOTES.md`
