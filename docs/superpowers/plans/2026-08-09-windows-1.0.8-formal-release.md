# Windows 1.0.8 Formal Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将已经验证的 Windows 2.1.158 TMDB 海报网络修复整理为 Windows 1.0.8 正式版，生成并验证 EXE，只向 GitHub 发布 Windows 资产。

**Architecture:** 保留 2.1.158 的功能实现，将版本契约、用户弹窗、版本历史和 README 切换到 Windows 1.0.8 正式版。Android 手机继续使用 1.0.4，Android TV 继续暂停；构建与发布阶段只调用 Windows Release 和 Inno Setup EXE 流程。

**Tech Stack:** Flutter 3.41.9、Dart、PowerShell、Inno Setup、Git、GitHub CLI

---

## 文件范围

- Modify: `pubspec.yaml` — Windows 正式版与历史 MSIX 兼容版本。
- Modify: `lib/core/app_version.dart` — 应用当前版本。
- Modify: `UPDATE_DIALOG_COPY.md` — Windows 1.0.8 正式版弹窗与 Android 不打包边界。
- Modify: `RELEASE_NOTES.md` — Windows 1.0.8 正式版说明，保留 2.1.158 测试版历史。
- Modify: `lib/utils/version_history.dart` — Windows 正式版当前历史及 Android 1.0.4 映射。
- Modify: `README.md` — Windows 1.0.8 下载入口；Android 1.0.4 链接保持指向 v1.0.7。
- Modify: `tool/android/build_signed_release.ps1` — 只同步 Windows pubspec 版本校验，不运行脚本。
- Modify: `test/release_config_contract_test.dart` — 正式版、弹窗原文与平台范围契约。
- Modify: `test/version_consistency_test.dart` — Windows 1.0.8、构建号 10008 与 Android 1.0.4 一致性。
- Modify: `test/version_history_current_test.dart` — Windows 正式版历史和 Android 当前历史映射。
- Modify: `test/android_release_packaging_test.dart` — Android 脚本的 Windows pubspec 前置版本。
- Modify: `test/android_tv_release_contract_test.dart` — TV 暂停契约中的 Windows pubspec 版本。
- Modify: `test/identity_v2_zero_residue_test.dart` — 当前 Windows 版本和构建号。
- Create locally: `build/release-assets-v1.0.8/KanYingYin-1.0.8.exe` — 唯一 GitHub Release 资产，不提交 Git。

### Task 1: 先更新正式版契约测试

**Files:**
- Modify: `test/release_config_contract_test.dart`
- Modify: `test/version_consistency_test.dart`
- Modify: `test/version_history_current_test.dart`
- Modify: `test/android_release_packaging_test.dart`
- Modify: `test/android_tv_release_contract_test.dart`
- Modify: `test/identity_v2_zero_residue_test.dart`

- [ ] **Step 1: 将当前 Windows 版本期望切换为 1.0.8**

测试常量和文本契约使用以下值：

```dart
const expectedVersion = '1.0.8';
const expectedBuildNumber = '10008';
const expectedAndroidVersion = '1.0.4';
const expectedAndroidVersionCode = '10004';
```

`release_config_contract_test.dart` 从 `## 1.0.8+10008` 截取到 `## 2.1.158+20158`，并断言当前说明包含 `Windows 正式版`、`仅 Windows 正式版 EXE`、`Android 当前版本：1.0.4 (10004，本轮不打包)`。

- [ ] **Step 2: 添加用户指定弹窗原文契约**

在 `release_config_contract_test.dart` 和 `version_history_current_test.dart` 中使用同一常量：

```dart
const posterProxyCopy =
    '改善 TMDB 海报的网络连接。部分网络可以正常获取影片资料，但无法直接下载海报；遇到这种情况时，可保持 Clash Verge 等本机代理在后台运行，并选择能够访问 TMDB 图片的节点。关闭系统代理不影响已经运行的本机代理，但完全退出代理软件后，海报可能再次无法加载。';
```

断言 `UPDATE_DIALOG_COPY.md`、当前 `RELEASE_NOTES.md` 段落和 Windows 1.0.8 版本历史均完整包含该字符串，不允许拆分或改写。

- [ ] **Step 3: 固定 Android 和 TV 不发布契约**

测试必须继续断言：

```dart
expect(androidGradle, contains('val androidVersionName = "1.0.4"'));
expect(androidGradle, contains('val androidVersionCode = 10004'));
expect(updateDialogCopy, contains('Android 应用版本：1.0.4'));
expect(updateDialogCopy, contains('本轮不打包 Android'));
```

`android_release_packaging_test.dart` 只把脚本的 Windows 前置版本改为 `1.0.8+10008`；`android_tv_release_contract_test.dart` 继续断言 TV 发布暂停，且不新增任何 TV 构建入口。

- [ ] **Step 4: 运行测试并确认先失败**

Run:

```powershell
D:\flutter\bin\flutter.bat test --no-pub `
  test\release_config_contract_test.dart `
  test\version_consistency_test.dart `
  test\version_history_current_test.dart `
  test\android_release_packaging_test.dart `
  test\android_tv_release_contract_test.dart `
  test\identity_v2_zero_residue_test.dart
```

Expected: FAIL，失败原因指向当前源码仍为 2.1.158 或缺少 Windows 1.0.8 正式版弹窗原文。

### Task 2: 同步 Windows 1.0.8 版本和用户文案

**Files:**
- Modify: `pubspec.yaml`
- Modify: `lib/core/app_version.dart`
- Modify: `UPDATE_DIALOG_COPY.md`
- Modify: `RELEASE_NOTES.md`
- Modify: `lib/utils/version_history.dart`
- Modify: `README.md`
- Modify: `tool/android/build_signed_release.ps1`

- [ ] **Step 1: 更新 Windows 版本源**

`pubspec.yaml` 使用：

```yaml
version: 1.0.8+10008
```

历史兼容字段使用：

```yaml
msix_version: 1.0.8.0
```

`lib/core/app_version.dart` 使用：

```dart
static const String current = '1.0.8';
```

`tool/android/build_signed_release.ps1` 的 Windows 前置校验同步为 `1.0.8+10008`，但本轮不得执行该脚本。

- [ ] **Step 2: 写入 Windows 弹窗和 Release Notes**

`UPDATE_DIALOG_COPY.md` 顶部必须使用：

```markdown
- 应用版本：1.0.8
- Windows EXE 安装器版本：1.0.8 正式版
- 本轮交付：仅 Windows 正式版 EXE；不打包 Android
- Android 应用版本：1.0.4
- Android versionCode：10004
```

弹窗标题为 `看影音 1.0.8 正式版`。Windows 正文完整包含用户指定的 `posterProxyCopy`，并保留统一海报网络、失败时保留资料和不修改原文件的说明。

`RELEASE_NOTES.md` 在 2.1.158 之前新增 `## 1.0.8+10008`，明确 `Windows EXE 安装器版本：1.0.8`、`Android 当前版本：1.0.4 (10004，本轮不打包)` 和 `### Windows 正式版更新内容`。2.1.158 历史段落保持不变。

- [ ] **Step 3: 新增 Windows 1.0.8 版本历史**

在 `lib/utils/version_history.dart` 中将 Windows 1.0.8 正式版放在 2.1.158 之前：

```dart
VersionHistory(
  version: '1.0.8',
  date: '2026-08-09',
  changes: [
    posterProxyCopy,
    '本地媒体库、个人网盘和手动匹配候选使用统一的 TMDB 海报网络设置',
    '海报暂时下载失败时，已经获取的标题、简介、评分和季集信息会继续保留，不影响媒体扫描和播放',
    '本次更新不会修改、删除、改名或移动本地及个人网盘中的原始视频和字幕',
  ],
),
```

实际实现可直接写入字符串，不新增跨文件常量。`versionHistoryForCurrent('1.0.8', platform: AppPlatformKind.android)` 必须继续返回 Android 1.0.4 正式版历史。

- [ ] **Step 4: 更新 README 下载入口**

Windows 当前版本、系统信息、安装步骤和下载链接改为 `1.0.8`、`v1.0.8`、`KanYingYin-1.0.8.exe`。删除“当前开发测试版为 Windows 2.1.158”的提示。

Android 两个下载链接继续指向：

```text
https://github.com/ddddd-ren/KanYingYin/releases/download/v1.0.7/KanYingYin-1.0.4.apk
https://github.com/ddddd-ren/KanYingYin/releases/download/v1.0.7/KanYingYin-1.0.4.aab
```

- [ ] **Step 5: 运行正式版契约测试并确认通过**

Run Task 1 Step 4 的相同命令。

Expected: PASS，0 failures。

- [ ] **Step 6: 提交正式版版本契约**

```powershell
git add -- pubspec.yaml lib/core/app_version.dart UPDATE_DIALOG_COPY.md RELEASE_NOTES.md lib/utils/version_history.dart README.md tool/android/build_signed_release.ps1 test/release_config_contract_test.dart test/version_consistency_test.dart test/version_history_current_test.dart test/android_release_packaging_test.dart test/android_tv_release_contract_test.dart test/identity_v2_zero_residue_test.dart
git commit -m '发布：准备Windows 1.0.8正式版'
```

Expected: 提交只包含 Windows 1.0.8 版本、文案和对应契约测试。

### Task 3: 回归验证 TMDB 海报网络实现

**Files:**
- Test: `test/tmdb_image_client_test.dart`
- Test: `test/tmdb_network_image_test.dart`
- Test: `test/tmdb_image_network_contract_test.dart`
- Test: `test/tmdb_endpoint_policy_test.dart`
- Test: `test/poster_service_test.dart`
- Test: `test/network_infrastructure_test.dart`

- [ ] **Step 1: 运行海报网络聚焦测试**

```powershell
D:\flutter\bin\flutter.bat test --no-pub `
  test\tmdb_image_client_test.dart `
  test\tmdb_network_image_test.dart `
  test\tmdb_image_network_contract_test.dart `
  test\tmdb_endpoint_policy_test.dart `
  test\poster_service_test.dart `
  test\network_infrastructure_test.dart
```

Expected: PASS，覆盖统一图片客户端、代理重试、公开图片回退、证书边界和缓存行为。

- [ ] **Step 2: 核对没有扩大网络安全边界**

Run:

```powershell
rg -n "badCertificateCallback|api_key|image\.tmdb\.org|http://image\.tmdb\.org" lib/services/tmdb lib/widgets/tmdb_network_image.dart lib/services/poster_service.dart
```

Expected: 不存在接受无效证书的回调；HTTP 回退只用于 `image.tmdb.org` 公开图片，不携带 TMDB API Key。

### Task 4: 执行完整质量门禁和 Windows Release 构建

**Files:**
- Verify: entire repository
- Generate: `build/windows/x64/runner/Release/kanyingyin.exe`

- [ ] **Step 1: 运行完整测试**

```powershell
D:\flutter\bin\flutter.bat test --no-pub
```

Expected: 所有测试通过，0 failures。

- [ ] **Step 2: 运行静态分析**

```powershell
D:\flutter\bin\flutter.bat analyze --no-pub
```

Expected: `No issues found!`

- [ ] **Step 3: 构建 Windows Release**

```powershell
D:\flutter\bin\flutter.bat build windows --release --no-pub
```

Expected: 构建成功；不得调用 Android、MSIX 或 TV 构建脚本。

- [ ] **Step 4: 验证 Release 主程序版本**

```powershell
$exe = Get-Item -LiteralPath 'build\windows\x64\runner\Release\kanyingyin.exe'
if ($exe.VersionInfo.ProductVersion -ne '1.0.8') {
  throw "Release 产品版本错误：$($exe.VersionInfo.ProductVersion)"
}
```

Expected: 产品版本为 1.0.8。

### Task 5: 生成并验证 Inno Setup EXE

**Files:**
- Generate: `build/windows/exe_output/看影音-1.0.8-测试版-安装程序.exe`
- Generate: `C:\Users\asus\Desktop\看影音-1.0.8-测试版-安装程序.exe`
- Create locally: `build/release-assets-v1.0.8/KanYingYin-1.0.8.exe`

- [ ] **Step 1: 运行 Windows EXE 交付脚本**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tool\windows\build_exe_release.ps1
```

Expected: Windows Release 和 Inno Setup 成功；安装程序复制到桌面；不生成 MSIX。

- [ ] **Step 2: 核对安装器版本、哈希和签名状态**

```powershell
$installer = Get-Item -LiteralPath "$env:USERPROFILE\Desktop\看影音-1.0.8-测试版-安装程序.exe"
$hash = Get-FileHash -LiteralPath $installer.FullName -Algorithm SHA256
$signature = Get-AuthenticodeSignature -LiteralPath $installer.FullName
[pscustomobject]@{
  Path = $installer.FullName
  Size = $installer.Length
  ProductVersion = $installer.VersionInfo.ProductVersion
  SHA256 = $hash.Hash
  Authenticode = $signature.Status
}
```

Expected: 产品版本 1.0.8；记录实际大小、SHA-256 和签名状态，不把 `NotSigned` 描述为已签名。

- [ ] **Step 3: 创建唯一 Release 资产**

```powershell
$assetDir = 'build\release-assets-v1.0.8'
New-Item -ItemType Directory -Path $assetDir -Force | Out-Null
Copy-Item -LiteralPath "$env:USERPROFILE\Desktop\看影音-1.0.8-测试版-安装程序.exe" -Destination "$assetDir\KanYingYin-1.0.8.exe" -Force
Get-ChildItem -LiteralPath $assetDir -File
```

Expected: 目录中只有 `KanYingYin-1.0.8.exe`，没有 APK、AAB、TV 或 MSIX 文件。

- [ ] **Step 4: 确认未自动安装新版**

重新查询卸载注册表、`D:\看影音\kanyingyin.exe` 产品版本和 `Get-AppxPackage`。

Expected: 已安装 EXE 仍为 2.1.158；旧 MSIX 仍不存在。

### Task 6: 整合到 main 并推送代码与标签

**Files:**
- Git branch: `codex/fix-tmdb-image-network-2.1.158`
- Git branch: `main`
- Git tag: `v1.0.8`

- [ ] **Step 1: 复核提交和工作区**

```powershell
git status --short
git log --oneline main..HEAD
git diff --check main..HEAD
```

Expected: 工作区干净；分支包含 2.1.158 海报网络修复、正式版设计、实施计划和 1.0.8 版本提交。

- [ ] **Step 2: 快进本地 main**

```powershell
git switch main
git merge --ff-only codex/fix-tmdb-image-network-2.1.158
```

Expected: `main` 快进到已验证提交，无合并提交和冲突。

- [ ] **Step 3: 推送 main**

使用 Git Credential Manager 的已授权凭据；如直连 GitHub 超时，使用已验证的 `ghfast.top` 代理并只在当前进程添加认证头。

Expected: 远端 `refs/heads/main` 与本地 HEAD 完全一致。

- [ ] **Step 4: 创建并推送标签**

```powershell
git tag -a v1.0.8 -m '看影音 1.0.8 正式版'
```

推送标签后核对 `refs/tags/v1.0.8^{}` 指向与 `main` 相同的提交。

### Task 7: 创建、校验并发布 GitHub Release

**Files:**
- Upload: `build/release-assets-v1.0.8/KanYingYin-1.0.8.exe`

- [ ] **Step 1: 创建草稿 Release**

使用 `RELEASE_NOTES.md` 当前 1.0.8 段落作为正文：

```powershell
gh release create v1.0.8 `
  --repo ddddd-ren/KanYingYin `
  --title '看影音 1.0.8 正式版' `
  --notes $notes `
  --draft
```

Expected: 草稿、非 prerelease。

- [ ] **Step 2: 上传唯一 Windows 资产**

```powershell
gh release upload v1.0.8 `
  'build\release-assets-v1.0.8\KanYingYin-1.0.8.exe' `
  --repo ddddd-ren/KanYingYin `
  --clobber
```

Expected: 只有一个资产，名称为 `KanYingYin-1.0.8.exe`。

- [ ] **Step 3: 对比远端大小和 SHA-256**

查询草稿 Release 的 asset `name`、`size` 和 `digest`，与 Task 5 的本地文件逐项比较。

Expected: 名称、大小和 `sha256:` 摘要完全一致；没有 APK、AAB、TV 或 MSIX。

- [ ] **Step 4: 发布并设为最新版**

只有 Step 3 完全一致后，才将 `draft` 改为 `false`、`prerelease` 保持 `false`，并设为 latest。

- [ ] **Step 5: 最终公开校验**

查询 `repos/ddddd-ren/KanYingYin/releases/latest` 并确认：

- 标签为 `v1.0.8`
- 标题为 `看影音 1.0.8 正式版`
- `draft=false`
- `prerelease=false`
- 资产数量为 1
- 唯一资产为 `KanYingYin-1.0.8.exe`
- 远端 `main`、标签解引用和本地 HEAD 指向相同提交
- 本地工作区干净
