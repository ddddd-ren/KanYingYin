# Windows 1.0.9 and Android 1.0.5 Formal Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将本地 2.1.159 至 2.1.169 的成果整理为 Windows 1.0.9 与 Android 1.0.5 正式版，完成双平台构建验证并向 GitHub 发布 EXE、APK、AAB 三项资产。

**Architecture:** 保留现有功能实现，只调整正式版版本源、平台化更新文案、版本历史和发布契约。Windows 弹窗综合 Windows 可感知更新；Android 弹窗综合当前完整源码中的 Android 与共享业务更新，但排除天玑 930、vivo Y77、夸克专项优化和 Windows 自动更新。Android TV 源码保留，任何 TV 构建和发布流程均不得运行。

**Tech Stack:** Flutter 3.41.9、Dart、Kotlin Gradle、PowerShell、Inno Setup、Android SDK build-tools、Git、GitHub CLI

---

## 文件范围

- Modify: `pubspec.yaml` — Windows 1.0.9 与历史 MSIX 兼容版本。
- Modify: `lib/core/app_version.dart` — Windows 当前应用版本。
- Modify: `android/app/build.gradle.kts` — Android mobile 1.0.5 与 versionCode 10005；Windows pubspec 前置契约 1.0.9。
- Modify: `tool/android/build_signed_release.ps1` — Android 1.0.5 签名构建与 Windows 1.0.9 前置契约。
- Modify: `UPDATE_DIALOG_COPY.md` — Windows 1.0.9 与 Android 1.0.5 两套正式版弹窗。
- Modify: `RELEASE_NOTES.md` — 新增双平台正式版段落，保留 2.1.159 至 2.1.169 历史。
- Modify: `lib/utils/version_history.dart` — 新增 Windows 1.0.9、Android 1.0.5 正式版历史与平台映射。
- Modify: `README.md` — 三项正式版资产的版本和下载链接。
- Modify: `test/release_config_contract_test.dart` — 双平台正式版与弹窗边界。
- Modify: `test/version_consistency_test.dart` — Windows/Android 版本一致性。
- Modify: `test/version_history_current_test.dart` — 两个平台的当前正式版历史。
- Modify: `test/android_release_packaging_test.dart` — Android 1.0.5 签名打包契约。
- Modify: `test/android_tv_release_contract_test.dart` — TV 暂停与当前版本契约。
- Modify: `test/identity_v2_zero_residue_test.dart` — Windows 1.0.9 当前身份版本。
- Modify as required by existing assertions: `test/about_page_content_test.dart` — 关于页当前版本与手动更新入口。
- Generate locally: `build/release-assets-v1.0.9/KanYingYin-1.0.9.exe`。
- Generate locally: `build/release-assets-v1.0.9/KanYingYin-1.0.5.apk`。
- Generate locally: `build/release-assets-v1.0.9/KanYingYin-1.0.5.aab`。

### Task 1: 用失败测试定义双平台正式版契约

**Files:**
- Modify: `test/release_config_contract_test.dart`
- Modify: `test/version_consistency_test.dart`
- Modify: `test/version_history_current_test.dart`
- Modify: `test/android_release_packaging_test.dart`
- Modify: `test/android_tv_release_contract_test.dart`
- Modify: `test/identity_v2_zero_residue_test.dart`
- Modify if needed: `test/about_page_content_test.dart`

- [ ] **Step 1: 将版本契约改为 Windows 1.0.9 与 Android 1.0.5**

测试常量使用：

```dart
const expectedWindowsVersion = '1.0.9';
const expectedWindowsBuildNumber = '10009';
const expectedAndroidVersion = '1.0.5';
const expectedAndroidVersionCode = '10005';
```

当前 Release Notes 从 `## 1.0.9+10009` 截取到 `## 2.1.169+20169`。断言当前段落包含 `Windows 和 Android 手机正式版更新内容`、两个版本号和三个交付格式。

- [ ] **Step 2: 定义 Windows 弹窗内容契约**

Windows 当前文案至少包含以下用户可感知内容：

```dart
const windowsReleaseCopy = <String>[
  '实时网速',
  '季度目录',
  '逐集名称',
  '手动匹配',
  'OpenList',
  'HTTPS',
  '海报',
  'GitHub',
  'SHA-256',
  '杜比视界',
  'HDR10+',
  '杜比全景声',
];
```

Windows 当前历史和 `UPDATE_DIALOG_COPY.md` 必须包含这些内容，并且不得出现 Android 设备专项描述。

- [ ] **Step 3: 定义 Android 弹窗内容与排除项契约**

Android 1.0.5 当前历史和弹窗必须包含：

```dart
const androidReleaseCopy = <String>[
  '高刷新率',
  '实时网速',
  '季度目录',
  '逐集名称',
  '手动匹配',
  'OpenList',
  'HTTPS',
  '海报',
  '杜比视界',
  'HDR10+',
  '杜比全景声',
  'TMDB',
];
```

Android 当前文案必须排除：

```dart
const androidForbiddenCopy = <String>[
  '天玑 930',
  'vivo Y77',
  '十路',
  '专项读取',
  'GitHub 自动检查',
  'Windows 版',
  'EXE',
  '系统代理',
  'Android TV',
];
```

- [ ] **Step 4: 固定三项资产和 TV 暂停边界**

README 与发布说明契约应包含：

```text
KanYingYin-1.0.9.exe
KanYingYin-1.0.5.apk
KanYingYin-1.0.5.aab
```

`android_tv_release_contract_test.dart` 继续断言 `AGENTS.md` 包含 Android TV 无限期暂停和不得运行 `tvTest`。不得新增或调用 TV 发布脚本。

- [ ] **Step 5: 运行聚焦测试并确认因旧版本失败**

```powershell
D:\flutter\bin\flutter.bat test --no-pub `
  test\release_config_contract_test.dart `
  test\version_consistency_test.dart `
  test\version_history_current_test.dart `
  test\android_release_packaging_test.dart `
  test\android_tv_release_contract_test.dart `
  test\identity_v2_zero_residue_test.dart `
  test\about_page_content_test.dart
```

Expected: FAIL，失败原因只指向源码仍为 2.1.169、缺少 1.0.9/1.0.5 正式版历史或弹窗边界尚未同步。

### Task 2: 同步版本源与双平台用户文案

**Files:**
- Modify: `pubspec.yaml`
- Modify: `lib/core/app_version.dart`
- Modify: `android/app/build.gradle.kts`
- Modify: `tool/android/build_signed_release.ps1`
- Modify: `UPDATE_DIALOG_COPY.md`
- Modify: `RELEASE_NOTES.md`
- Modify: `lib/utils/version_history.dart`
- Modify: `README.md`

- [ ] **Step 1: 更新 Windows 与 Android 版本源**

`pubspec.yaml`：

```yaml
version: 1.0.9+10009
```

历史兼容字段：

```yaml
msix_version: 1.0.9.0
```

`lib/core/app_version.dart`：

```dart
static const String current = '1.0.9';
```

`android/app/build.gradle.kts`：

```kotlin
val androidVersionName = "1.0.5"
val androidVersionCode = 10005
```

该文件和 `tool/android/build_signed_release.ps1` 的 Windows pubspec 前置检查同步为 `1.0.9+10009`；脚本内 Android 版本同步为 `1.0.5` 与 `10005`。

- [ ] **Step 2: 写入 Windows 1.0.9 正式版弹窗**

`UPDATE_DIALOG_COPY.md` 顶部使用：

```markdown
- 应用版本：1.0.9
- Windows EXE 安装器版本：1.0.9 正式版
- Android 应用版本：1.0.5
- Android versionCode：10005
- 本轮交付：Windows 正式版 EXE、Android 正式版 APK/AAB
```

Windows 标题为 `看影音 1.0.9 正式版`。正文综合实时网速、季度目录识别、逐集名称、手动匹配保留、OpenList HTTPS、网盘海报稳定显示、GitHub 自动更新和媒体技术标签。

- [ ] **Step 3: 写入 Android 1.0.5 正式版弹窗**

Android 标题为 `看影音 Android 1.0.5 正式版`。正文综合高刷新率、实时网速、季度目录与逐集名称、手动匹配保留、OpenList HTTPS、网盘海报稳定显示、媒体技术标签和 TMDB 海报失败边界。

正文不得写天玑 930、vivo Y77、夸克专项读取、Windows GitHub 自动更新、EXE、系统代理或 Android TV。

- [ ] **Step 4: 新增 Release Notes 和版本历史**

在 `RELEASE_NOTES.md` 的 2.1.169 之前新增：

```markdown
## 1.0.9+10009

Windows EXE 安装器版本：1.0.9

APK/AAB 版本：1.0.5 (10005)

### Windows 和 Android 手机正式版更新内容
```

分别使用 `#### Windows 1.0.9` 与 `#### Android 1.0.5`，严格保持平台文案边界。

在 `lib/utils/version_history.dart` 中新增 Windows 1.0.9 正式版条目和独立的 Android 1.0.5 正式版常量。`versionHistoryForCurrent('1.0.9', platform: AppPlatformKind.android)` 返回 Android 1.0.5；Windows 返回 1.0.9。

- [ ] **Step 5: 更新 README 三项下载入口**

README 使用：

```text
https://github.com/ddddd-ren/KanYingYin/releases/download/v1.0.9/KanYingYin-1.0.9.exe
https://github.com/ddddd-ren/KanYingYin/releases/download/v1.0.9/KanYingYin-1.0.5.apk
https://github.com/ddddd-ren/KanYingYin/releases/download/v1.0.9/KanYingYin-1.0.5.aab
```

系统信息更新为 Windows 1.0.9、Android 1.0.5 (10005)。Android TV 暂停说明保持不变。

- [ ] **Step 6: 运行聚焦测试并确认通过**

重复 Task 1 Step 5 命令。

Expected: PASS，0 failures。

- [ ] **Step 7: 提交正式版版本契约**

```powershell
git add -- pubspec.yaml lib/core/app_version.dart android/app/build.gradle.kts tool/android/build_signed_release.ps1 UPDATE_DIALOG_COPY.md RELEASE_NOTES.md lib/utils/version_history.dart README.md test/release_config_contract_test.dart test/version_consistency_test.dart test/version_history_current_test.dart test/android_release_packaging_test.dart test/android_tv_release_contract_test.dart test/identity_v2_zero_residue_test.dart test/about_page_content_test.dart
git commit -m '发布：准备1.0.9与安卓1.0.5正式版'
```

### Task 3: 执行完整质量门禁

**Files:**
- Verify: entire repository

- [ ] **Step 1: 运行完整测试**

```powershell
D:\flutter\bin\flutter.bat test --no-pub
```

Expected: 所有测试通过，0 failures。使用至少 6 分钟执行窗口，避免主动关闭测试输出管道。

- [ ] **Step 2: 运行静态分析**

```powershell
D:\flutter\bin\flutter.bat analyze --no-pub
```

Expected: `No issues found!`

- [ ] **Step 3: 检查工作区副作用**

```powershell
git status --short
git diff --check
```

Expected: `pubspec.lock` 未变化，不存在 `.dart_appdata` 或构建工具写入的跟踪文件。

### Task 4: 构建并验证 Windows 1.0.9 EXE

**Files:**
- Generate: `build/windows/x64/runner/Release/kanyingyin.exe`
- Generate: `C:\Users\asus\Desktop\看影音-1.0.9-测试版-安装程序.exe`
- Create locally: `build/release-assets-v1.0.9/KanYingYin-1.0.9.exe`

- [ ] **Step 1: 构建 Windows Release**

```powershell
D:\flutter\bin\flutter.bat build windows --release --no-pub
```

Expected: 构建成功，Release 主程序产品版本和文件版本均为 1.0.9。

- [ ] **Step 2: 生成 Inno Setup EXE**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tool\windows\build_exe_release.ps1
```

Expected: 安装程序复制到桌面，不生成 MSIX。

- [ ] **Step 3: 记录 Windows 资产信息**

```powershell
$installer = Get-Item -LiteralPath "$env:USERPROFILE\Desktop\看影音-1.0.9-测试版-安装程序.exe"
$hash = Get-FileHash -LiteralPath $installer.FullName -Algorithm SHA256
$signature = Get-AuthenticodeSignature -LiteralPath $installer.FullName
```

验证产品版本为 1.0.9，记录大小、SHA-256 和 Authenticode 实际状态。若状态为 `NotSigned`，发布说明不得称其已签名。

### Task 5: 构建并验证 Android mobile 1.0.5

**Files:**
- Generate: `C:\Users\asus\Desktop\看影音-1.0.5.apk`
- Generate: `C:\Users\asus\Desktop\看影音-1.0.5.aab`
- Create locally: `build/release-assets-v1.0.9/KanYingYin-1.0.5.apk`
- Create locally: `build/release-assets-v1.0.9/KanYingYin-1.0.5.aab`

- [ ] **Step 1: 检查 Android 签名环境**

确认 `KANYINGYIN_ANDROID_KEYSTORE`、`KANYINGYIN_ANDROID_STORE_PASSWORD`、`KANYINGYIN_ANDROID_KEY_ALIAS`、`KANYINGYIN_ANDROID_KEY_PASSWORD` 在用户或进程环境中存在，且 keystore 文件可访问。不得输出密码或私钥内容。

- [ ] **Step 2: 只构建 mobile APK/AAB**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tool\android\build_signed_release.ps1 -Flavor mobile
```

Expected: APK 与 AAB 构建、签名和复制成功。不得运行 `tool\android\build_tv_test.ps1`，不得传入 `-Flavor tvTest`。

- [ ] **Step 3: 验证 Android 包身份与签名**

使用 Android SDK 最新 build-tools 的 `aapt.exe` 与 `apksigner.bat` 核对 APK：

- package：`com.kanyingyin.player`
- versionName：`1.0.5`
- versionCode：`10005`
- APK v2 签名有效

使用 JDK `jarsigner.exe -verify -strict` 和本机 keystore 验证 AAB。记录 APK 签名证书 SHA-256，不输出签名密码。

- [ ] **Step 4: 验证 Full libmpv 三种 ABI**

构建脚本必须成功调用 `tool/android/verify_full_media_bundle.ps1`，APK 与 AAB 均包含并校验：

```text
arm64-v8a
armeabi-v7a
x86_64
```

记录每种 ABI 的 `libmpv.so` SHA-256。

### Task 6: 创建并校验三项 Release 资产

**Files:**
- Create locally: `build/release-assets-v1.0.9/*`

- [ ] **Step 1: 建立空资产目录**

若目录已存在文件，先列出并停止，避免覆盖未知产物。确认空目录后，将桌面 EXE、APK、AAB 复制并重命名为：

```text
KanYingYin-1.0.9.exe
KanYingYin-1.0.5.apk
KanYingYin-1.0.5.aab
```

- [ ] **Step 2: 逐项核对大小和 SHA-256**

源文件与资产目录副本必须逐项完全一致。目录内文件数量必须为 3，不得包含 TV、MSIX 或其他历史安装包。

- [ ] **Step 3: 复核安装状态**

重新查询卸载注册表、`D:\看影音\kanyingyin.exe` 产品版本和 `Get-AppxPackage`。

Expected: 本机仍安装 2.1.169，旧 MSIX 不存在；本轮不自动安装 1.0.9。

### Task 7: 提交、推送 main 与 v1.0.9 标签

**Files:**
- Git branch: `main`
- Git tag: `v1.0.9`

- [ ] **Step 1: 最终复核并提交剩余相关变更**

```powershell
git status --short
git diff --check
git diff --stat
```

只提交本轮版本、文案、契约和设计计划相关文件；构建产物保持被 `.gitignore` 忽略。

- [ ] **Step 2: 推送 main**

使用 Git Credential Manager 已授权凭据。若直连 GitHub 超时，使用已验证的 `ghfast.top` 代理，并只在当前进程添加认证头。

Expected: 远端 `refs/heads/main` 与本地 HEAD 完全一致，包含 v1.0.8 后尚未推送的所有本地提交。

- [ ] **Step 3: 创建并推送标签**

```powershell
git tag -a v1.0.9 -m '看影音 1.0.9 正式版'
```

推送后核对 `refs/tags/v1.0.9^{}`、远端 `main` 和本地 HEAD 指向同一提交。

### Task 8: 创建、校验并公开 GitHub Release

**Files:**
- Upload: `build/release-assets-v1.0.9/KanYingYin-1.0.9.exe`
- Upload: `build/release-assets-v1.0.9/KanYingYin-1.0.5.apk`
- Upload: `build/release-assets-v1.0.9/KanYingYin-1.0.5.aab`

- [ ] **Step 1: 从 Release Notes 提取正式版正文**

只提取 `## 1.0.9+10009` 到 `## 2.1.169+20169` 之前的内容，作为 GitHub Release 正文。

- [ ] **Step 2: 创建草稿 Release**

```powershell
gh release create v1.0.9 `
  --repo ddddd-ren/KanYingYin `
  --title '看影音 1.0.9 正式版' `
  --notes $notes `
  --draft
```

Expected: 草稿、非 prerelease。

- [ ] **Step 3: 上传三项资产**

使用 `gh release upload v1.0.9 ... --clobber` 上传 EXE、APK、AAB。不得上传 TV、MSIX 或源码外的其他安装包。

- [ ] **Step 4: 比较远端名称、大小和 SHA-256**

通过 GitHub API 读取草稿 Release。三项远端资产必须与本地逐项一致；资产数量必须为 3。

- [ ] **Step 5: 发布并设为最新版**

只有 Step 4 完全通过后，才将 `draft` 改为 `false`、`prerelease` 保持 `false`，并设为 latest。

- [ ] **Step 6: 最终公开审计**

确认：

- latest 标签为 `v1.0.9`
- 标题为 `看影音 1.0.9 正式版`
- 资产数量为 3
- 唯一资产集合为 EXE、APK、AAB 指定名称
- GitHub Release 正文的 Android 部分不含天玑 930、vivo Y77、夸克专项优化或 Windows 自动更新
- 远端 `main`、标签解引用和本地 HEAD 一致
- 本地工作区干净
- 本机安装版本仍为 2.1.169
- 不声称 Android 真机播放验收已完成
