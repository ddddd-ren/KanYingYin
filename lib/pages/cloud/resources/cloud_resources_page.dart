import 'package:flutter/material.dart';

class CloudResourcesPage extends StatelessWidget {
  const CloudResourcesPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('网盘资源')),
      );
}
